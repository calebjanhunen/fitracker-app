import React from 'react';
import { Text, View } from 'react-native';

export default function WorkoutTrackerFormScreen(): React.ReactElement {
    return (
        <View>
            <Text>WorkoutTrackerForm</Text>
        </View>
    );
}
