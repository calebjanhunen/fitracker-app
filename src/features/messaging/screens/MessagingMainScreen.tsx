import React from 'react';
import { Text, View } from 'react-native';

export default function MessagingMainScreen(): React.ReactElement {
    return (
        <View>
            <Text>Messaging</Text>
        </View>
    );
}
